// Local system font stacks (no network fetch at render time — Liberation
// Sans is metric-compatible with Arial/Helvetica and has full Vietnamese
// glyph coverage). Heavy weight + uppercase + letter-spacing carries the
// bold, modern-masculine look instead of a novelty display font.
export const displayFontFamily =
  '"Liberation Sans", Arial, Helvetica, sans-serif';
export const bodyFontFamily =
  '"Liberation Sans", Arial, Helvetica, sans-serif';
